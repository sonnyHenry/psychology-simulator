# Agent Handoff

给后续接手的模型快速同步进度。**先读这一份,再读 `packages/core/FORK.md`。**

## 项目定位

《心理学模拟器》:2014 年高考报志愿 → 本科四年 → 硕/博/直博 → 博后 → 教职求职季 → 预聘期 → 2033 长聘首考。

- 学术线 = 第一优先级,临床线 = 第二优先级,两条都做一级精度;其余路径粗做但必须可达。
- 游戏的高光是**教职求职季**(国内 + 海外双市场)。
- 设计目标:让心理学从业者感同身受——包括做科研的、做咨询的、和离开了的。

设计文档已定稿到可开工的完整度:[GAME_DESIGN.md](./GAME_DESIGN.md)(1186 行)· [TECH_ARCHITECTURE.md](./TECH_ARCHITECTURE.md)(942 行)。**改机制前先读对应章节,不要凭直觉加字段。**

## 里程碑状态

| 里程碑 | 状态 |
|---|---|
| M0 fork 与骨架 | ✅ 完成 |
| M1 DSL 与阶段扩展 | ✅ 完成 |
| M2 开局与本科(地基) | ✅ 完成 |
| M2.5 毕业论文教学关 | ✅ 完成 |
| **M3 课题管线(一级)** | ✅ 完成 |
| M3.5 真实素材层 · M3.6 文献可靠性 | ⬜ 下一步 |
| M4 临床线 · M4.5 社会层 | ⬜ |
| M5 求职季与学术终局 | ⬜ |
| M6 二级线 · M7 元玩法与隐线 · M7.5 跨局差异 · M8 打磨 | ⬜ |

完整清单与验收标准见 TECH_ARCHITECTURE 第九节。

---

## M0 fork 与骨架(已完成)

**做了什么**

1. 从前作 `47cb3f4` 复制 `packages/core` + `packages/tools`,改名 `@psy-sim/*`,写 `packages/core/FORK.md`。
2. **五维换血**:学识→方法 · 心态→状态 · 人脉→资本 · 健康→临床 · 金钱不变。不是加维度,是整套替换。
3. **专业分流 → 学院归属**:`ApplicationMajor.trackFlag`(`flags.major_track`)→ `college`(`flags.college`),取值 science/education/medical/normal。
4. `tools/verify-npcs.ts` 改写成声明式夹具表 + 通用断言(前作那五段查的是前作专属 flag,fork 后必然全红)。夹具表现在是空的。
5. 新建 `packages/content` 骨架包:8 张特质卡(照抄设计文档表格)· 6 张背景卡 · 8 道高考题 · 4 档志愿(含四种学院归属)· 3 个骨架事件 · 1 个占位结局 · 一条 `gaokao → undergrad` 时间线。

**验收**

```
pnpm typecheck              3 个包全绿
pnpm test                   core 38/38 绿
pnpm validate               0 errors, 0 warnings
pnpm simulate -- -n 500     500/500 局跑完,事件覆盖 3/3
```

**接手时要知道的三件事**

1. **`engine.ts` 里还有一堆前作的内容硬编码**(考研/求职/考公三岔口、按专业名分流、`first_love` 强制 NPC、结局页 6 段前作 NPC 收束文案、十万/五十万/一百万里程碑)。M0 **刻意没动**——前作那 38 个单测正是靠它们才能证明 fork 是干净的。完整清单和"谁来清"在 [FORK.md](./packages/core/FORK.md#前作留下本作必须清掉的内容硬编码)。清的时候顺手把内容搬出引擎,不要原地改字符串。

2. **阶段路由现在按数组下标走**(`settleRound` 末尾 `enterPhase(state.phaseIndex + 1)`)。本作有六条并行培养路径,不修就会串线(直博阶段结束会掉进硕士阶段)。修法在 TECH 4.3/4.5,**这是 M1 的第一件事**。M0 的时间线因此只有一条链。

3. `packages/content/src/events/undergrad/m0-skeleton.ts` 和 `endings/index.ts` 是**明确要被删掉的占位内容**,不要在上面继续往下写。真正的本科内容按 GAME_DESIGN 第八节的结构分文件。

**M0 期间被抓到的一个真实回归**:`evalCondition` 的单测写死了 `mindset >= 70`(前作初始心态 70)。状态改成 65 之后它红了——换血确实需要一遍全量单测才敢说干净。

---

## M1 DSL 与阶段扩展(已完成)

**做了什么**

1. **阶段路由改成显式**:新增 `nextPhaseId`,`settleRound` 与 flow 阶段收尾都走 `enterNextPhase`。约束比 TECH 4.3 更严——**每个非终局阶段都必须写,一个不许漏**(validate 强制)。多个 `isFinal` 阶段放开。
2. `FlowStep` → **`StepScreen`**,flow 阶段的 `steps` 与 rounds 阶段的 `roundOpeners` 共用同一套进屏逻辑(`enterStep`/`nextStep`)。
3. `PhaseConfig` 新增 `roundOpeners` / `yearsPerRound` / `allocationSlots` / `nextPhaseId`。
4. DSL 新增 `{ flagNum }` 条件 + `{ addFlag }` / `{ extendPhase }` / `{ grantSlots }` 效果;读取口径统一在导出的 `readNumericFlag`。
5. 新增 `systems/allocation.ts`,导出 `effectiveSlots(state, phase)`——`grantSlots` 的唯一读取口径。
6. **validate 新增阶段路由规则 + TECH 规则 3(危机内容规范)与规则 4(累积量读写成对)**;规则 4 做成**双向**的(TECH 只要求"写了必须有人读",反方向抓的是 key 拼错)。
7. 新增 **`tools/verify-validate.ts`**:给每条新规则喂一个反例,断言它真的红。已并入 `pnpm test`。

**验收**

```
pnpm test    core 58/58 绿 · verify-validate 14/14 反例全部变红(含 1 条阳性对照)
```

核心验收是**六条培养路径不串线**:夹具 `routingPack()` 的 timeline 顺序故意排错(`phd_direct` 的数组后继是 `master`、`postdoc` 的是 `school`),断言直接写成 `expect(run.phases).not.toContain('master')`。

**接手时要知道的两件事**

1. **往时间线中间插阶段,必须同时重接两条边**(前驱的 `nextPhaseId` 指向新阶段、新阶段指向原后继)。只 `splice` 不改路由的话新阶段会被直接跳过。M1 期间三个前作单测就是这么红的。
2. **`{ addFlag }` 不算"可见数值变化"**,纯 `addFlag` 的 outcome 会被 validate 拦。这是故意的:OUTCOME 屏目前只展示 `stats` 变化,只涨 `clinical_hours` 的选项在玩家眼里是"点了没反应"。等 UI 能展示累积量再决定放开。

**规则 1 和 2 没做**:TECH 7.1 的规则 1(课题阶段图无死锁)和规则 2(个案状态图无死锁)依赖 `ProjectStage` / `CaseStatus` 和 `{ project }` / `{ case }` 这些 M3/M4 才存在的类型,现在写等于凭空猜 schema。**跟着 M3/M4 一起落地,并同时往 `verify-validate.ts` 加反例。**

---

## M2 开局与本科(地基)(已完成)

**做了什么**

1. **`ALLOCATION` 屏 + 课程系统**:本科每年四格精力,两门重点课占两格。课程三档判定
   (`P(学通) = 0.06 + 0.18×格数 + (属性−50)×0.006 + 小测 0.15`),产出**能力标签**而不是属性。
2. **两座大山的期末小测**:心理统计的 `p = .04` 那道题、实验心理学的混淆变量题。
   复用 `EXAM` 屏(新增 `state.examKind` 区分),**零新屏**。
3. **门槛开放时间不对称**(8.3②):实验室 2016 开门、咨询中心 2017 才开门,
   所以**实验室先开门整整一年**。这一年就是"做研究比做咨询更容易发生"的机制来源。
4. **51 个本科事件**:大一幻灭三连 · 大二"第一次看懂结果部分"(`tier: major`)· 四个时代节点变体池
   (各 ≥3 变体)· 学院归属四套专属 · 社会层三颗种子 · 极克制的危机事件 · "假装听懂"配对 · 耗竭读取点。
5. **大四三岔口内容驱动**:人生取向五选一(改评分权重)+ 七条路径(门控读本科四年攒下的东西)。
6. **`packages/web`**:14 种 ViewModel 全部渲染,含格数计量条式的分配屏。
7. **validate 新增规则 24、28–32** + 6 个反例;`simulate --check` 从 M2 起生效;
   `repetition` 新增"学院归属之间的成对重合率"。

**验收(三条标准全部达标)**

| 标准 | 结果 |
|---|---|
| 浏览器里能玩到 2018 大四三岔口 | ✅ `pnpm build` 通过,七条路径都能选到并走到结局 |
| 每个 `masteryFlag` 都有条件读它 | ✅ validate 规则 28(它当场抓到 4 个没人读的标签) |
| 四种学院归属的本科事件重合率 <60% | ✅ **46.8%**(同学院 58.0% 作为对照) |

```
pnpm test    core 69/69 · verify-validate 20/20
pnpm simulate -- -n 3000 --check   ✅ 覆盖 51/51 · 七条路径可达率 3.7%–21.9%
```

**接手时要知道的四件事**

1. **事件抽取在开场屏之后**(`enterBrief` 里),不在 `startRound` 里。改这个顺序会让投入分配整年失效,
   而且不报错——详见 [FORK.md 第 10 条](./packages/core/FORK.md)。
2. **给变体池选门控条件的纪律**:门控要么在窗口之前就已定(背景卡 / 特质 / 学院归属),
   要么这个变体永远等不到自己的窗口。M2 踩了一次:一个变体要 `mastered_stats`,
   而那个 flag 在**学年末**才写入,于是同组别的变体先触发、把整组关掉了。
   **这类 bug 不报错,只表现为"这个变体从来没人见过"。** M7.5 要把二十个时代节点全转成变体池,
   这条纪律那时候会被反复用到。
3. **`Course.masteryFlag` 是可选的。** 声明了就必须有人读(规则 28)。
   普通心理学"学通"不该解锁什么——它只是属性和年度回顾页上的一行。
   **这个字段的数量等于课程系统真正的因果链条数**,别为了整齐给每门课都加一个。
4. **社会层三颗种子现在是"叙事版"**:人情账用 `favor_owed_senior` 累积量、竞争者用 `rival_appeared` flag、
   情报用 `rumors_heard` 累积量。真实机制(`{ favor }` / `RivalState` / `RumorDef` + `ASK_AROUND`)在 M4.5。
   **M4.5 接管时改的是实现,不是内容。**

---

## M2.5 毕业论文教学关(已完成)

**做了什么**

1. **`Project` 进 `GameState`**——P5 的第一个真实使用者。配套 `{ projectCount }` 条件、
   `{ project }` 效果、`systems/project.ts`、`ProjectTemplate` 内容类型。
2. **毕业论文 = 裁短的课题管线**:`想法 → 收数据 → 分析 → 写作 → 答辩`,跳过伦理/预注册和投稿。
   五个阶段在**同一学年**内用 `{ schedule: { afterRounds: 0 } }` 串成链。
3. **`p = .08`**:四个选项(照实写 / 再加 20 个被试 / 换个假设 / 问导师),
   每一个在当时都说得通。`integrity_risk` 从这里开始记账,并**结转到毕业之后**
   (由记账的那个 outcome 排一个 `afterRounds: 1` 的结转确认事件)。
4. **修掉一处前作 bug**:跳阶段时残留 `pendingFlowAdvance`,会在回合中间重抽事件队列。
5. **validate 规则 1**(课题阶段图无死锁)+ 4 个反例。
6. 年度回顾页开始显示课题(标题 / 阶段 / 第几年)。

**验收**

```
pnpm test    core 78/78 · verify-validate 24/24
pnpm simulate -- -n 3000 --check   ✅ 覆盖 57/57
```

管线走完率 97.9%;诚信账结转非零 76.7%(随机 bot)。

**接手时要知道的三件事**

1. **阶段语义:一个事件就是那个阶段的工作。** `trigger` 读 `{ projectCount: { stage: X } }`
   = "课题正卡在 X",outcome 用 `advance` 推进出去。**差一格就会让课题永远停在最后一个阶段上**
   ——M2.5 真的差了一格,而且是手写 trace 发现的,不是 validate。现在规则 1 会抓。
2. **`{ schedule }` 的事件绕过 `trigger` 检查。** 所以"只有满足条件才出现"这件事**不能靠 trigger 挡**,
   必须由排它的那一方决定要不要排(诚信结转事件就是这么做的:只有真的记了账的 outcome 才排它)。
3. **`applyEffects` 拿不到 RNG,而且不该拿。** 引擎偷偷消耗随机流会让同种子的回放漂移,
   所以课题标题按创建序号轮取、阶段推进不做随机判定(成败由内容侧的 outcome 权重决定)。
   这条纪律在 M3 写九阶段管线时会被反复用到。

**M3 要接的三件事**:`Paper` 类型 + `publish` 操作 · 调度器的管线阶段事件钩子(TECH 4.5 的 ②',
现在毕业论文靠 schedule 串链就够,多个并行课题时才需要)· 阶段推进的成功率判定。

**一个要盯的数字**:`overseas_phd` 的可达率在 2.7%–3.7% 之间浮动,而 M6 的门禁是"每条路径 ≥3%"。
那条门禁还没接进 `--check`,接的时候先看这个数。

---

## M3 课题管线(一级)(已完成)

**做了什么**

1. **骰子由引擎掷,故事由内容讲。** 推进由 `stageSuccessChance` 决定
   (阶段基准 × 导师原型 + 方法 + 投入格数 + 质量),阶段事件只讲故事、改质量/诚信、
   提供"放弃 / 改设计"这类转折。内容用 `{ projectRoll }` 分流文案,所以话永远和骰子一致。
2. **`Paper` + 自动发表**,档位由 `quality` 决定,`integrityRisk` 结转。
   结局页有了两份清单:**你的论文** 和 **没做完的**。
3. **六个导师原型**,`archetype` 绝不进 ViewModel(有单测钉住)。`projectModifiers` 是乘数。
4. **调度器 ②'/③'**:管线阶段事件(每轮 ≤3,投入过的课题优先)+ 导师关系事件(≤1)。
5. **17 个阶段事件**,文案全部参数化(`{{project}}` / `{{years}}` / `{{advisor}}`)。
6. **硕士 3 年 → 硕士岔口 → 博士 3 年,以及直博 5 年**,六个博士毕业结局。
7. **年度状态回复由耗竭决定**——耗竭螺旋不是直接扣状态,是掐掉恢复能力。
8. `ADVISOR_DRAW` + `PROJECT_BOARD` 两个屏;`--check` 新增三条学术线门禁。

**验收**

```
pnpm test    core 91/91 · verify-validate 25/25
pnpm simulate -- -n 3000 --check   ✅ 覆盖 83/83
```

四种 bot 的实测见 [FORK.md 的标定结果](./packages/core/FORK.md)。一句话:
**"至少一个课题彻底做废"在四种打法下全部 ≥70%**,而论文数的梯度(集中 2.46 / 分散 1.1)
说明"会不会分配精力"就是"会不会做研究"。

**已知差距**:TECH 7.2 的"平均论文数 3–9"目前只在中位数上达到(`--bot score` p50=3,均值 2.46)。
差的那部分在博后(M5)——真实世界里产出最高的两三年。**M5 做完后重新量这个数。**

**接手时要知道的四件事**

1. **阶段事件不推进课题。** 它们只讲故事。推进是引擎的事,写内容时不要往 outcome 里塞 `advance`
   ——那会让失败率不再是系统性的(毕业论文是唯一的例外,它是教学关)。
2. **`{{project}}` 靠 `state.eventProjects` 找到"这个事件在说哪个课题"。**
   被 `schedule` 排进来的阶段事件没有这个绑定,`boundProjectId` 会按事件声明的阶段兜底找一个。
3. **"上限 + 大量掷骰"会把随机机制悄悄变成确定机制。** M3 标定时踩得最狠的一个坑:
   每年掷 6 次、封顶 2 站,结果推进站数恒等于 2,于是奇数站的内容一次都不出现。
   **要方差就得少掷,不能靠上限压。** 任何"每年 N 次、封顶 M"的设计都有这个坑。
4. **管线是六站不是九站**(伦理并进文献,投稿并进写作)。原因是时间预算,推导写在
   `content/src/projects/index.ts` 的文件头。加站之前先算一遍"每年一两站 × 学制几年"。

---

## 两条必须从头遵守的纪律

这两条不是"以后再优化",事后补的成本是当初就那么写的三到五倍(前作在第 41–49 轮补了九轮变体池,那就是学费):

1. **管线阶段文案一律参数化**——引用课题名 / 年数 / 导师 / 竞争者,不要写死。
2. **每个 mandatory 时代节点从写第一版起就是变体池**(≥3 个成员,按情境区分而不是靠 `chance`)。

见 TECH_ARCHITECTURE 第九节 M7.5 下面那段说明。


---

## M3.1 实机反馈修复(玩家打通一局之后报回来的七个问题)

**七个问题里六个静态检查查不出来。** validate 全绿、91 个单测全过的情况下,
玩家仍然在实机里连着撞上七处。这一轮的教训比修复本身重要。

| 现象 | 根因 |
|---|---|
| 硕士分配屏出现"进实验室搬砖(大二就能进)""备考";"休息"出现两次 | 本科分配项**只写 `from` 不写 `to`**,学生工作/兼职/休息干脆没门控 |
| 分配项说明里的 `**强调**` 显示成星号 | `Allocation.tsx` 直接渲染裸文本,没走 `RichText` |
| 说"你找到了三个人"却只能问一个 | 师兄根本没有选项,师姐的选项被 flag 挡住,而开场白不认这回事 |
| 相似的开题选择连着出现两次 | `ev_first_real_project`(课题<3)和 `ev_open_another_project`(分配屏驱动)**触发条件完全重叠** |
| 同一幕阶段事件重复出现 | 阶段事件是 `once:false`,而调度器只在**同一轮内**去重,跨年不记账 |
| 硕士毕业(2021)进入"大厂用研"显示 2019 年 | 五个 pathStub 被大四岔口和硕士岔口**共用**,而它们把 `date` 写死成 2019 |

### 三条留下来的规矩

1. **`availableWhen` 必须有上界。** 只写 `from` 的门控会一路漏到后面所有阶段。
2. **被多个入口共用的阶段不许写 `date`。** 省略即沿用当前日期(`PhaseConfig.date` 现在是可选的)。
   同理,这类阶段的 brief 里不许出现具体年月。
3. **一个决定只问玩家一遍。** 两个事件的触发条件重叠时,先划清"谁负责哪一种情形"再写文案。
   现在的分工:第一个课题由剧情给 · 之后每一个都要在分配屏花一格换 · 还有一个是导师塞的。

### 顺带发现:两条**死规则**

修 `ev_first_real_project` 之后做废率从 89.4% 掉到 53.9%,查下去发现
**原来那 89.4% 有一部分是那个重复 bug 撑起来的**——引擎不管玩家想不想都塞三个课题,
没投精力的自然烂掉。补上真实的第三个课题来源(`ev_advisor_hands_project`,导师塞给你的,
而且推不掉的概率是一半)之后回到 73.7%,四个 bot 全部 ≥70%。

查这个的过程里挖出两条**从来没生效过**的常量:

- `MAX_YEARS_BEFORE_WRITING = 6`——硕博全程一共 6 年,一个研二开的课题永远够不到。
  改成 4 之后随机 bot 做废率 70.3% → 74.4%。
- `MAX_REJECTIONS = 3`——3000 局里 `rejections` 的分布是 `{0: 323, 1: 1}`,**一个都没被拒死过**。
  原因不在阈值:`review` 是管线最后一站,课题在这一站推进成功就直接发表,
  于是从来没有课题会"停在审稿上过一年"。**"投了四个刊都没中"在游戏里等于不存在。**
  这和 M3 那个"817 次伦理审查、0 次收数据"是同一个病:管线只是路过的那一站,
  它的内容和机制都不会发生。真修法是把发表改成 review 站上一次独立的接收判定(失败即一次被拒),
  那会动到论文产出,**要配一轮完整标定,单独排**。

> **规矩:每个阈值都要放回时间线里核一遍够不够得到,并且配一条 simulate 指标盯着它。**
> 静态检查查不出死常量,只有统计能。

### 已知残留

`--bot score`(集中投入)的做废率 56.6%,低于 GAME_DESIGN 的 ≥70%。
集中打法把每个课题都照顾到,而现在课题只会死于"没人管"——**认真做但做不出来的课题还杀不掉**,
那正好是上面 `MAX_REJECTIONS` 那条要修的东西。`--check` 的门禁走随机 bot(73.7%),暂时通过。


---

## M3.2 节奏:每年放几幕

玩家反馈"每一年的事件太多了,玩久了有点累"。**在此之前没有任何一条指标在看这个**——
`simulate` 量论文数、做废率、状态曲线,唯独不量"一年放几幕"。现在有了:

```
每年事件数中位数(括号内 p90):
改前  2015:5  2016:4  2017:8(9)  2018:8(10)  2019:4  2020:3  2021:4  2022:2  2023:2  2024:2
改后  2015:5  2016:4  2017:5(7)  2018:8(10)  2019:4  2020:3  2021:4  2022:3  2023:2  2024:2
```

### `eventSlots` 不是"随机槽位数",是**总幕数上限**

```ts
while (picked.length < phase.eventSlots)   // picked 里已经装了 mandatory
```

大三有 8 幕 mandatory,`picked.length` 一开始就 ≥ 3,于是那一年**一个随机事件都不抽**。
这一条同时解释了两件事:

1. **把 `eventSlots` 从 3 砍到 2 几乎没用**(2017 仍是 8)——槽位本来就没在填。
2. 砍到 2 之后**事件覆盖从 84/84 塌到 64/84**:那些年份里唯一的随机机会也没了。

**所以减少每年事件数的正确杠杆是减 mandatory,不是减槽位。** 本科的内容量按 3 格配,砍槽位只会让内容不可达。

### `mandatory` 的判断标准

**只有"不发生游戏就会坏掉或说谎"的才配 mandatory:** 进组抽导师(门控导师系统)、
时代节点(设计纪律)、毕业论文开题、课题创建、危机事件。
课程氛围、学院色彩、NPC 种子这些应该去和随机槽位抢——它们不出现,游戏依然成立。

照这条降级了 4 幕,大三从 8 降到 5。

### 踩到的坑:降级会连带掐死下游

`ev_u1_weber`(第一次专业课考试)看着像纯氛围幕,**但它是唯一设置 `reads_outside_syllabus` 的地方**,
而 2016 年那个时代节点变体读这个 flag。降级它之后,它自己和
`ev_era_2016_replication_online` **一起变成不可达**。

> **降级一幕之前先查它写了哪些 flag,以及谁在读。** 静态检查现在查不出这个。

### 顺带修的 bug:被 schedule 的事件不认 `once`

毕业论文那笔诚信账(`ev_thesis_ledger`)被六处 outcome 各 `schedule` 了一次,
而 schedule 路径**完全绕过 `isEligible`**(那正是它的意义:不受 trigger 和池子约束),
于是记了两笔账的玩家会连着看两遍同一幕。现在 schedule 也认 `once`,
`once: false` 的事件(阶段事件、开题那几幕)不受影响。

### 还没动的:大四仍是 8 幕

大四 = 开题 + 时代节点 + **毕业论文教学关的 5–6 幕** + 槽位。
那 5–6 幕就是 M2.5 的教学关本身(想法 → 收数据 → p=.08 → 写作 → 答辩 → 诚信账),
**砍它等于砍掉教毕业论文流程的那一关**。大四是本科的高潮年,密度高是设计选择,
不是失控——但如果要继续压,这里是唯一剩下的地方。


---

## M3.3 让"投稿被拒"这条死法真的开火(M3.1 记下的那笔账)

M3.1 记过:`MAX_REJECTIONS` 是条死规则,3000 局里 `rejections` 的分布是 `{0: 323, 1: 1}`。
现在它是 **23%**——三条引擎死法(投稿被拒 / 烂在手里 / 做太久)全部在开火,
而且**四个 bot 的做废率第一次全部达标**。

| bot | 平均论文 | 清单为空 | 至少一个课题做废 |
|---|---|---|---|
| `score` | 1.28 | 15.0% | **74.1%** |
| `random` | 1.74 | 9.6% | **76.4%** |
| `money` | 1.50 | 12.6% | **88.2%** |
| `state` | 1.16 | 21.1% | **93.0%** |

### 改了什么

1. **推进只推到最后一站为止。** 原来是"推过最后一站就发表",于是课题从来不会
   **停在**审稿上——到达那一年的骰子成功一次就直接发出去了,`rejections` 永远加不上去。
2. **每年一次接收判定。** 站在最后一站的课题,每年掷一次"接不接收":接收即发表,
   被拒就留在审稿上明年再投一家,被拒够 `MAX_REJECTIONS`(现在是 2)次就不投了。
3. **接收判定是独立公式 `acceptanceChance`,不是 `stageSuccessChance` 加一层。**
   - **不含投入格数。** 推进可以靠多投精力加速,审稿不行——投出去之后你能做的事很少。
   - **质量斜率是推进的两倍**(0.012 vs 0.006)。立论不结实的文章,再努力也只是被拒得慢一点。

### 走过的两条弯路(都量过,别再走)

**弯路一:让接收判定吃掉一年。** 到达审稿先等一整年再判。结果论文数从 1.66 掉到 1.05、
清单为空涨到 20%,而**被拒率仍然是 0%**——博士生涯只有六年,第四年才投出去的课题
最多攒两次拒稿。**这条时间线付不起"等一年"。** 所以接收判定放在到达当年,不额外吃年份。

**弯路二:`acceptanceChance` 建在 `stageSuccessChance` 上。** 后者含 `investedSlotsOn`,
于是"多投精力刷不过审稿"这句话在实现里是假的——**注释写着一套,代码在做另一套。**
是新加的单测抓到的(同样投入 0 格和 2 格,接收率必须相等)。

> **规矩:声称"某个东西不影响它"的时候,写一条单测把那句话钉住。**
> 注释不会被执行,而这一条正好是整个机制的要点。

### 顺带

- `simulate` 新增两条常驻指标:**每年事件数**(节奏)和**课题做废的死法分布**。
  后者的第一版把阈值抄了一份(写死 `>= 3`),`MAX_REJECTIONS` 调成 2 之后
  它把被拒死的课题算进了"玩家主动放弃"——**指标自己撒了谎**。现在一律从引擎导入。
- "结局到达 12/13"里少的那个是**兜底结局**(`always: true`),它到不了才是对的。
  输出里现在标出来了,省得下次又有人追。
